{
  "name": "backend",
  "version": "1.0.0",
  "description": "See or Skip application",
  "engines" : {
    "node" : "12.14.1"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha --recursvie --exit",
    "build" : "cd client && npm run build",
    "client" : "cd client && npm start",
    "install-client" : "cd client && npm install",
    "heroku-postbuild" : "npm run install-client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.10"
  }
}